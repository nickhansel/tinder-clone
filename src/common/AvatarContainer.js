import React from "react";
import PropTypes from "prop-types";
import { AvatarStyled } from "./styles";

const AvatarContainer = ({ strategy }) => {
  return <AvatarStyled />;
};

export default AvatarContainer;
